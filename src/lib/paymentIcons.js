import { Banknote, CreditCard, Landmark, Smartphone } from "lucide-react";

// A curated icon set shared by checkout and the admin payment-method
// editor, so every icon an admin can pick is guaranteed to render.
export const PAYMENT_ICONS = { Banknote, Smartphone, Landmark, CreditCard };
